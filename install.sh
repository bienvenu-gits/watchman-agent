#!/bin/bash

# Update package list and install nmap
sudo apt-get update
sudo apt-get install -y nmap

# Install nmap using yum (CentOS/RHEL)
# sudo yum install -y nmap

# Install nmap using dnf (Fedora)
# sudo dnf install -y nmap

# Install nmap using pacman (Arch Linux)
# sudo pacman -Sy nmap --noconfirm